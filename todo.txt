TODO List:

Status : Parsing commands with arguments and redirections.

a) write an auxiliary function called skip_whitespaces: DONE


1) Write structure for executing the commands after parsing them (fork, dup2, exec). : DONE
2) Feature to support pipes. (For each parser a list of commands) : DONE
3) Background execution
4) Signals
5) Wild Characters
6) Aliases

Convetions:
a) Specific grammar for command's format
b) cat file.txt | sort < file* doesn't allowed (input stream for a pipelined command)